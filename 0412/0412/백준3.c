#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int N;
	int num[1000001];
	int max, min;
	scanf("%d", &N);

	for (int i = 0; i < N; i++) {
		scanf("%d", num[i]);
	}

	for (int i = 0; i < N; i++) {
		if (num[i] == max);
		if (num[i] == min);
	}
	printf("%d %d", &max, &min);
}